#include "Coord.h"
#include <assert.h>
#include <iostream>

using namespace std;

int main(int argc, char const *argv[]){
	/*
        Coord a,b;
	a.x = 1;
	a.y = 1;
	b.x = 1;
	b.y = 1;
	assert(manhattanDist(a,b,1,1) == 0);
	b.y = 2;
	
	assert(manhattanDist(a,b,3,3) == 1);
	b.x = 2;
	assert(manhattanDist(a,b,3,3) == 2);
	b.x = 4;
	b.y = 1;

	X X X X X 
	X A X X B 
	X X X X X 
	X X X X X 
	X X X X X 
	
	assert(manhattanDist(a,b,5,5) == 2);
	a.x = 0;
	
        X X X X X 
	A X X X B 
	X X X X X 
	X X X X X 
	X X X X X 

	assert(manhattanDist(a,b,5,5) == 1);
	*/
	return 0;
}
